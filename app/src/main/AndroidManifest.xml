<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".Play.QuoteActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>


        <activity android:name=".first_use.SetName"/>
        <activity android:name=".first_use.SetFirstNotification"/>

        <activity android:name=".nawigation.ManageNawigation"/>
        <activity android:name=".onboarding.OnBoardingActivity"/>
        <activity android:name=".settings.Theme"/>

        <activity android:name=".MainActivity"/>
        <activity android:name=".first_use.SetFirstLanguage"/>
        <activity android:name=".Play.Summary"/>



    </application>

</manifest>